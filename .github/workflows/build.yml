name: Build LaTeX

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build LaTeX
    runs-on: ubuntu-latest
    container: texlive/texlive

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Thesis
        run: latexmk -pdfxe -shell-escape ctufit-thesis.tex

      - name: Build Presentation
        run: latexmk -pdfxe -cd presentation/ctufit-presentation.tex

      - name: Rename PDF Files
        run: |
          mv ctufit-thesis.pdf dp-paukert.pdf
          mv presentation/ctufit-presentation.pdf dp-paukert-presentation.pdf

      - name: Upload Thesis Files
        uses: actions/upload-artifact@v4
        with:
          name: dp-paukert-pdf
          path: ./dp-*.pdf
